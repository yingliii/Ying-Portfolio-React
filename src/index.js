import React from 'react';
// importing ReactDOM from 'react-dom' dependency
import ReactDOM from 'react-dom';
// importing component App
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
